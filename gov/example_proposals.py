# Example proposals that can be used for developing & debugging

proposals = list()

demo = dict()
demo.update({"name": "demo1"})
demo.update({"type": "budget"})
demo.update({"abstract": "abstract demo"})
demo.update({"background": "background demo"})

demo2 = dict()
demo2.update({"name": "demo2"})
demo2.update({"type": "budget"})
demo2.update({"abstract": "abstract demo"})
demo2.update({"background": "background demo"})

proposals.extend([demo, demo2])
